{
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "start": "cd server && yarn start",
    "start:frontend-dev": "cd client && react-scripts start",
    "build": "cd client && rm -rf build/ && react-scripts build"
  }
}
